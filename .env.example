# Path to your photos directory (required)
PHOTOS_DIR=/home/user/Pictures

# OpenVINO device: NPU, GPU, or CPU (default: NPU with auto-fallback)
NPU_DEVICE=NPU

# Number of dominant colors to extract per image
NUM_COLORS=5

# Skip already-processed files (set to false to reprocess all)
SKIP_EXISTING=true
